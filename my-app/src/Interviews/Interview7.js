import React from 'react'

const Interview7 = () => {
    //Polyfill is a custom code that we write for already 
    // existing method in case the browser doesnt support it.

    // function check() {
    //     return 100;
    // }
    // var check;
    // console.log(check); //check function (priority)

    //handling asynchronous operations
    //-promises
    //-async/await
    //-setTimeout (other callback functions)

    //Let vs const: both block scope with let allowing 
    // value to be redeclared multiple times while const
    //  will not allow that and needs to be initialized at
    //  the first declaration with let it can be empty

    //var is function scope that can be reassigned being 
    // hoisted and being able to be accessed outside of block
    //  scope

    //first class functions
    //functions that can be treated like any other variable---
    // -assigned, passed or returned

    //data types in JS
    //-String (primitive)
    //-Number (primitive)
    //-boolean
    //-object (Non Primitive)
    //-array(Non primitive)

    //Optional chaining is a way to safely access nested
    //  object properties without causing errors if a 
    // property doesn’t exist.

    return (
        <div>

        </div>
    )
}

export default Interview7
